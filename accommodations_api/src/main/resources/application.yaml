spring:
  application:
    name: accommodations_api

  datasource:
    driver-class-name: org.postgresql.Driver

  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        show_sql: true
  profiles:
    active: dev

server:
    port: 9090